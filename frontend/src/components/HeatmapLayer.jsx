// TODO (Day 20): Leaflet heatmap layer
// Renders crime/risk data as a heatmap on the map
// Accepts: dataPoints (array of {lat, lng, intensity})
export default function HeatmapLayer() {
  return null;
}
