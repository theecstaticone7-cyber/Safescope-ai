// TODO (Day 19): Main query page
// User types area + time + persona (Hindi or English)
// Calls backend → ai-service → displays risk score cards + RAG explanation + map preview
export default function AskSafeScope() {
  return null;
}
