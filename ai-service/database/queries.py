# TODO (Day 10): SQL query functions
# get_area_stats(city, area) → aggregated risk stats from NCRB data
# get_hotspot_cache(city) → pre-computed hotspot coordinates
# get_time_window_stats(area, hour_range) → incident counts by time slot
