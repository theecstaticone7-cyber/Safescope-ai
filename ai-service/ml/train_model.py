# TODO (Days 6-8): Train both ML models on NCRB data
# 1. Risk Classifier (XGBoost + Random Forest) → risk_classifier.pkl
# 2. Incident Regressor (XGBoost + Random Forest) → incident_regressor.pkl
# Saves both models to models/ folder
