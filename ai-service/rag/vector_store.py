# TODO (Day 12): Build and persist FAISS vector index
# build_index(docs) — creates FAISS index from document embeddings
# save_index() — saves to models/vector_store/
# load_index() — loads existing index at startup
